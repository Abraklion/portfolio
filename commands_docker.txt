


$ docker-compose exec back python manage.py flush --no-input
$ docker-compose exec back python manage.py migrate

$ docker-compose -f docker-compose.prod.yml down -v
$ docker-compose -f docker-compose.prod.yml up -d --build
$ docker-compose -f docker-compose.prod.yml exec back python manage.py migrate --noinput


docker-compose exec db psql --username=microb --dbname=portfolio_prod
\l
\c hello_django_dev
\dt
\q

================= test ============================
$ docker-compose -f docker-compose.prod.yml up -d --build
$ docker-compose -f docker-compose.prod.yml exec back python manage.py migrate --noinput
$ docker-compose -f docker-compose.prod.yml exec back python manage.py collectstatic --no-input --clear


dev
===
docker compose up --build - запустить
docker compose build --no-cache - сборка не из кэша
docker compose exec back python manage.py createsuperuse
docker compose down       - стоп
docker compose down -v    - стоп и удалить вольюмы
